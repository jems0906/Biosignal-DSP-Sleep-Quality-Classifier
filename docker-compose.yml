services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    environment:
      VITE_API_URL: http://localhost:8000
    depends_on:
      - backend
